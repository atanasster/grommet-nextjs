// eslint-disable-next-line no-underscore-dangle
export const _starter = `const Demo = () => (
  <Notification
    message='Notification'
    onClose={() => {}}
    timestamp={new Date()}
    percentComplete={30}
    strong={true}
    status='warning'
  />  
);

render(<Demo />);  
`;
