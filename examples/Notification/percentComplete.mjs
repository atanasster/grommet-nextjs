export const percentComplete = `const Demo = () => (
  <Notification
    status='ok'
    message='Task percentage'
    percentComplete={80}
    size='small'
    onClose={() => {}}
  />
);

render(<Demo />);  
`;
